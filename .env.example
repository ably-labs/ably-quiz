# Copy to .env.local (gitignored) and fill in what you have.
# Missing keys are skipped gracefully — see CLAUDE.md.

# --- Models (S0 spike onwards) ---
ANTHROPIC_API_KEY=          # day 0: this is all you need to start (S0 spike)
OPENAI_API_KEY=             # later — Matt GPT
XAI_API_KEY=                # later — Matt Grok

# --- Ably (needed from S1) ---
ABLY_API_KEY=               # server-side only; never NEXT_PUBLIC_*

# --- App ---
HOST_KEY=                   # quiz-creation secret; any long random string

# --- MCP grounding (S6, week 2) ---
ABLY_MCP_URL=
ABLY_MCP_AUTH=
