{
  "name": "ably-quiz-spike-sim",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Drive a real quiz through Ably: host (core Quizmaster + web adapters) + N synthetic players. Verifies the S3.3 loop and is the S3.6 load harness.",
  "scripts": {
    "sim": "tsx sim.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@ably-quiz/core": "workspace:*",
    "ably": "^2.24.0",
    "dotenv": "^17.4.2"
  },
  "devDependencies": {
    "tsx": "^4.23.0"
  }
}
