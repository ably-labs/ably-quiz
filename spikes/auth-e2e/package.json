{
  "name": "ably-quiz-spike-auth-e2e",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "S1 gate: prove /api/ably-auth JWTs authenticate against real Ably and enforce the capability matrix (§B2.5).",
  "scripts": {
    "verify": "tsx verify.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "ably": "^2.0.0",
    "dotenv": "^17.4.2"
  },
  "devDependencies": {
    "tsx": "^4.23.0"
  }
}
